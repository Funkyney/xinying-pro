# 交付验收记录

更新时间：2026-08-21

本文件记录“心影Pro”目标的可重复验收证据。未完成的真实平台门槛不得用单元测试替代。

| 目标 | 当前证据 | 状态 |
|---|---|---|
| Electron + React + TypeScript + Playwright + SQLite | `package.json`、`src/main`、`src/renderer`、`src/core/database.ts`；`npm run typecheck` 通过 | 已验证 |
| Windows 可运行与安装包 | `npm run smoke:electron` 通过；正式免安装目录启动正常，NSIS 安装包构建完成 | 已验证 |
| APP 内加载心影与独立会话 | `WebContentsView` 使用 `persist:xinying-platform`；实机飞书扫码登录后，重打包并重启最新免安装版，仍直接进入 `/home` 且诊断为 `xinying-authenticated` | 已验证 |
| 个人/团队空间与项目 | 0.3.0 实机同步读到个人空间 2 个项目、团队空间 81 个项目；APP 可搜索、切换并显示私有/团队共享语义。个人空间成功创建 `APP功能验证-20260821`，随后重新同步发现同名项目而未重复创建；项目进入 `/avpAgent?projectId=…` 后创作区就绪 | 已端到端实机验证 |
| 不使用未公开接口 | 源码搜索仅发现结果 URL 下载与本地文件协议；网页动作均在 `playwright-adapter.ts` 中通过可见 DOM 完成 | 已验证 |
| 项目与参数管理 | Seedance 2.5 / 2.0 为一等模型选项；实机读取心影面板后按模型联动模式、比例、分辨率和时长。2.5：最高 1080p、4–30 秒；2.0：支持 4k、4–15 秒；SQLite 迁移与错误组合拒绝测试通过 | 已实机验证 |
| 参考图交互 | 缩略图、拖拽、编号、用途、单项替换、等量批量替换；批量替换按文件名自然排序并在写入前确认 `@图N ← 文件` 映射；0.3.3 将单图删除改为 APP 内确认框并隔离拖拽指针事件，删除后自动补齐编号；文件替换可回滚；任务入队生成专属不可变文件快照 | 已验证 |
| 人像与参考图统一排序 | APP 允许认证虚拟人像与本地图片、视频、音频任意穿插并持久化。心影会把多个 V 角色强制重组且顺序不受点击先后控制；适配器现在一次批量选择全部角色，通过稳定 `platformAssetId` 回读每个角色的真实 `@图N/@视频N`，再把提示词中的 APP 编号无碰撞映射为心影编号 | 两个人像计划顺序 `f69d… → ad192…` 被心影显示为 `ad192… → f69d…`，APP 已把示例提示词 `@图1/@图2` 自动改写为 `@图2/@图1`；另用 3 个人像 + 1 张本地图真实提交成功进入心影任务，未再出现选择确认错误 |
| 参考图映射到心影 | 用户授权后，在 APP 独立会话中按 1→2→3 上传三张纯色编号 PNG；素材槽位计数依次为 1、2、3，最终 DOM 顺序精确为“图1、图2、图3”。提示词长度保持 0、用户/代理消息保持各 10 条，未触发生成；验证后已将三项测试草稿全部清理，最终素材数为 0 | 已实机验证 |
| 虚拟人像 | 既有合成头像已完成上传、承诺、审核和 `approved` 验收；0.3.5 又用当前参考图完成“授权中 → 已授权”实机验收，自动填写三个“其他”、国内范围与合规承诺，后台表单不覆盖 APP，同一参考图重复点击不会重复建任务。0.3.2 团队空间同步 1,391 个常用角色，并完整读取 269 个当前账号可删除角色；管理模式支持最新/最早、批量选择与双重确认 | 已实机验证，未执行删除 |
| 任务查询、结果预览与下载 | 对话以 `sessionId + userIndex` 定位，并在心影最近消息窗口滑动后以完整提示词回退匹配；点击生成前持久化 `pending-chat` 防重复；真实新提交任务完成配对、结果识别、Electron 自动下载、结果库预览和 CLI 另存，另存导出不改变结果库主文件 | 已端到端实机验证 |
| 人工暂停点 | 登录、扫码、验证码、实名、支付、无法验证的合规字段、页面变化和提交阶段异常退出均进入 `needs-login`/`needs-human`；用户明确在 APP 记住原创虚拟人像合规声明后，可自动代勾选心影承诺并提交 | 单元与实机 DOM 验证通过；真实飞书扫码与登录恢复已验证 |
| Codex JSON CLI | `xinying.cmd` 无 npm 日志前缀；支持 `platform catalog/sync/open/create`、按空间的人像同步/查询、参考图授权和 `portrait platform-delete`。永久删除缺少 `--confirm` 时输出结构化 `CONFIRMATION_REQUIRED`；成功、帮助和错误分支均通过纯 JSON 子进程验证 | 已验证 |
| 文档 | `README.md`、`AGENTS.md`、`docs/ADAPTER_MAINTENANCE.md`、`docs/SECURITY.md` | 已验证 |
| 完整源码归档 | `release/Xinying-Director-0.3.5-source.zip`；归档只包含源码、测试、脚本、配置、文档与构建资产，不包含会话、数据库、测试结果或依赖目录 | 已验证 |

## 当前自动化检查

```powershell
npm run typecheck
npm test
npm run build
npm run smoke:electron
npm run verify:cli
npm run verify:portraits
npm run verify:platform-projects
npm run verify:projects-ui
npm run pack:win
```

当前测试基线：8 个测试文件、56 项测试通过。除既有生成、恢复和 4K 能力外，新增覆盖统一 `materialOrder`、心影角色选择数量解析、角色真实编号映射、提示词编号无碰撞替换、项目门禁、个人/团队目录持久化与绑定、无首轮 `sessionId` 的新心影项目、空间级人像隔离、最新顺序、删除权限校验、删除后实时项目清单清理、中间参考图删除后的文件清理与编号补位、任务快照不追溯修改、旧缓存迁移、参考图合规授权幂等、默认人像资料及空间级稳定 ID。

最新桌面冒烟测试返回 `ok: true`、窗口标题“心影Pro”且生成工作台可见。APP 独立会话内只读打开既有 `avpAgent` 会话后，适配器命中 1 个生成区、1 个提示词区、1 个参考图输入、1 个素材列表、1 个模型开关、1 个参数开关；心影仅保留最近 10 组可见用户/代理消息，本次新任务在重载后从提交时索引 10 滑动到索引 9，适配器已通过完整提示词回退匹配到正确结果。

最新安装包：`release/Xinying-Director-0.3.5-x64.exe`，大小 `117,891,522` 字节，SHA-256：`6F5F583351229CDDB8DE241662A26167C904991893DADDCE50410481AC4FE77C`。当前未配置商业代码签名证书，签名状态为 `NotSigned`。

实机参考图证据保存在 `test-results/reference-upload-proof.json`。其中 `generated=false`、`submitButtonClicked=false`、`promptTextLength=0`，三项 `materials` 的索引与标签一一对应；清理脚本随后验证消息数未变化并将草稿恢复为空。

2026-08-21 经用户明确授权进行一次可能扣费的生成尝试。CLI 仅创建了一个任务 `9d9cff6b-a121-4774-8b27-d0b08d14b27c`；适配器在上传参考图和点击心影生成按钮之前发现当前模型不支持 `720p`，任务安全暂停，未产生平台提交或扣费，也未自动重试。随后该本地任务被取消以防误恢复；新项目改为默认 `resolution=auto`。该结果验证了不兼容参数保护，但不能替代一次真实生成/结果下载验收。

同日使用完全合成的成年头像完成虚拟人像端到端实机验收。任务 `3afeb279-4bf1-4830-ba7e-39ec222d2e2b` 先取得 `portrait-staged:*` 标识并在承诺与“确定”前暂停；用户明确确认必须勾选承诺后，按国内版范围人工完成提交。心影角色库随后显示“合成测试人像-0821”，新版适配器从首页自动进入角色库并按同名 `.faceCard` 识别，任务为 `completed`、素材为 `approved`。重打包并从正式 `release/win-unpacked` 重启后，飞书会话和上述状态均保持。结构化证据保存在 `test-results/authorized-live-run-proof.json`。

同日以既有历史结果做无生成、无扣费下载验收：APP 按 `sessionId + userIndex=9` 定位结果，识别完成状态，通过结果卡自身的可见下载控件触发真实 Electron `will-download` 事件，并把 `20,482,052` 字节文件保存到受管结果目录；任务事件记录“结果已保存到本地结果库”。仅记录大小和 SHA-256，未查看视频内容；验证后已删除临时文件和两条临时数据库记录，心影原结果未修改。结构化证据保存在 `test-results/existing-result-download-proof.json`。

可靠性加固后的正式候选版再次执行同一无生成回归：渲染沙箱开启、任务轮询串行化，仍只产生一次 `COMPLETED` 事件，文件大小和 SHA-256 与前次一致。候选版提升到正式 `release` 后从正式路径重启，飞书登录会话、虚拟人像名称/资料/已通过状态均保持；回归临时文件和数据库记录已再次清理。

同日用户给出后续受控测试可直接执行的持续授权，并接受本次最低成本生成可能扣费。任务 `00ffab65-563f-4caf-8c7b-b412c50e76bb` 使用三张编号色卡、Seedance 2.5 全能参考、16:9、5 秒、分辨率跟随心影当前值、无声。前三次恢复均在生成点击前暴露兼容问题并安全停止；修复后台视口、模型异步回填和时长控件后，只产生 1 条 `SUBMIT_INTENT_RECORDED` 和 1 次真实生成点击。心影返回 `1280*720` 结果；会话重载后最近消息窗口使索引从 10 漂移到 9，新版适配器按完整提示词重新配对并将最终标识更新为 `chat:<projectId>:<sessionId>:9`。

APP 随后通过该结果卡的下载控件自动保存 `2,309,347` 字节文件，SHA-256 为 `1B802393766E46F0B34145EB50BD620212270D9638A2DC2940A33754246C4D73`；未查看视频内容。结果库显示对应项目、提示词、受管媒体预览元素和下载按钮。CLI 另存副本的大小与哈希完全一致，且结果库主路径未改变；临时另存副本已删除。结构化证据保存在 `test-results/live-generation-proof.json`。

0.2.0 实机标定先后切换 Seedance 2.5 与 Seedance 2.0，只读其参数面板并恢复原页面：两者比例均为 9:16、16:9、4:3、1:1、3:4、21:9、自适应；2.5 显示 480p/720p/1080p 与 4–30 秒，2.0 显示 480p/720p/1080p/4k 与 4–15 秒。未点击生成。随后正式包执行 `npm run verify:portraits`，从“+V角色”认证库滚动同步 1,392 个角色，ID 全部唯一；临时本地项目使用 Seedance 2.0、4k、15 秒和一个同步角色，预览 `ready=true` 且角色为 `@图1`，校验后临时项目已删除。同步完成后平台页面恢复到原 `/home`，没有留下素材或提示词草稿。

0.3.0 实机标定确认首页 `.selector-trigger` 可在“个人空间（数据不与他人互通）”与“华东区中央BlueMaster”团队空间间切换，项目卡为 `.selector-project-item`，个人空间项目 2 个、团队项目 81 个。新建项目表单在个人空间只要求名称与视频创作类型，在团队空间额外要求所属客户；创作类型实测为短剧推广、电商APP推广、游戏推广、3C、化妆品、玩具、食品、汽车、其他。个人空间项目 `APP功能验证-20260821` 已真实创建，重试时通过同步目录复用，没有重复创建；新项目首次进入生成页时 URL 只有 `projectId`、尚无 `sessionId`，但创作区与模型/参数/上传控件均已就绪，因此 0.3.0 将 `sessionId` 改为首次提交后回读，而不是错误阻断新项目。

同一项目随后实机滚动同步 1,394 个个人空间可调用角色，旧版无空间归属的 1,392 条缓存被标记为不可用，避免重复显示。最新角色 `Lyra Aurel（莱拉·奥瑞尔）日常近景` 的 `sortOrder=0`，加入项目后预览为 `@图1` 且 `ready=true`；Seedance 2.0、4k、15 秒的临时本地快照也通过，校验后临时项目已删除。UI 证据 `platform-projects-ui.png`、`portraits-library-ui.png` 和 `studio-portrait-authorization-ui.png` 分别确认空间项目页、共享库“最新上传在前/加入当前项目”以及参考图“授权为虚拟人像”按钮；为 UI 验证导入的本地参考图随后已删除，未向心影提交人像审核，也未点击视频生成。结构化实机证据保存在 `test-results/platform-projects-proof.json`，其中 `generated=false`。

0.3.1 将已授权人像与本地图合并为同一个上方参考素材面板。结构化证据 `test-results/unified-material-ui.json` 记录完整“移除→点击加入→拖到首位→恢复”过程，截图 `test-results/unified-material-ui.png` 显示两张人像卡均有“已授权”标识并与本地图统一编号。项目数据库最终保存两个 `portrait:*` 后接一个 `reference:*` 的顺序；验证过程没有点击视频生成。心影草稿标定上传的一张既有参考图与两个已授权角色均在验证后清空。

0.3.2 新增共享虚拟人像批量管理。只读实机检查确认心影角色卡仅在 `canDelete` 条件成立时显示 `.icon-shanchu`，官方确认文案为“确定删除角色 / 删除后，角色将不可恢复。”。APP 通过可见筛选器切换“全部 / 上传人像”：普通库保持原有常用角色窗口，管理库完整读取当前账号上传结果并保存独立 `deleteSortOrder`，避免 6,370 个公共/团队角色使旧上传排到不可见区。团队空间最终缓存 1,391 个常用角色、269 个可删除角色；可删除顺序 0–270 且 269 个值唯一（两项上传结果无删除权限形成空位）。`portrait-batch-management-ui.png` 记录“最早上传优先”下全选 120 项后的确认框，清单从“输出2.png · 可管理上传最新第 271”开始；确认复选框未勾选，按钮保持禁用。验证脚本返回 `confirmationChecked=false`、`deleteInvoked=false`，没有删除线上角色，也没有提交生成。

0.3.3 修复统一参考素材卡片的删除交互。问题卡片确认是本地参考素材，而非心影共享虚拟人像；垃圾桶此前依赖浏览器原生同步确认框，在桌面窗口中反馈不直观。新版将卡片按钮与拖拽指针事件隔离，并使用 APP 内确认框显示准确的 `@图N`、缩略图、文件名和删除边界。实机点击用户当前 `@图3` 后确认框正常显示，再选择“取消”，四张真实素材保持不变；服务测试另在隔离临时目录中确认删除中间项会清理受管副本、把后续位置补齐并持久化新 `materialOrder`。

0.5.7 修复“心影未确认全部虚拟人像选择”。根因是旧版逐个人像重新打开角色库，却始终只接受“已选 1 项”；心影会保留已选项，第二个人像实际显示“已选 2 项”。新版在一个弹窗中一次性勾选任务所需的全部角色，等待并严格核验“已选 N 项”。由于心影还会按内部固定顺序重组角色，新版不再依赖点击顺序或素材卡位置，而是通过每个角色的稳定素材 ID 回读真实编号，并自动替换最终提交提示词中的编号。APP 任意混排仍完整保存，心影物理重排不会再改变角色语义。

0.3.5 将参考图授权改为卡片内状态流。当前参考图首次重复点击曾产生两条任务，较早重复项已取消；新版用 `sourceReferenceId` 持久关联参考图、人像与审核任务，后续点击会复用活动或已完成任务。实机任务 `c580d150-e118-492c-a96c-ed1f9786cba4` 自动选择性别/年龄/人种“其他”、国内版范围和合规承诺并成功提交，精确区分同名与 `_1` 角色后完成为 `approved`；生成工作台卡片先显示禁用的“授权中”，最终显示禁用的“已授权”。自动化期间心影视图移至屏幕外，恢复已提交任务时只核对角色库，不会再次上传。本次没有提交视频生成。

## 0.4.0 多媒体顺序与全项目结果库

- 心影实机文件入口确认：图片 `.jpeg/.jpg/.png`、视频 `.mp4/.mov`、音频 `.wav/.mp3`。APP 对三类媒体独立编号；最终音频卡显示 `@音频1`、“音频参考”和内嵌播放器，音频不会进入人像授权流程。
- 心影角色库实机同步 3,645 项可调用角色，其中 24 项预览 URL 为 `.mp4?x-tos-process=video/snapshot...`，自动识别为视频角色。角色“加斑马线”在心影素材槽实际返回 `视频1`；在 APP 已有另一项视频角色时加入它，统一素材卡实际显示 `@视频2 / 视频角色`，测试后项目 `portraitIds` 与 `materialOrder` 精确恢复。
- 0.5.7 实机以两个人像计划顺序 `f69d… → ad192…` 测试，心影固定显示为 `ad192… → f69d…`。APP 按素材 ID 得到计划角色实际标签 `@图2 → @图1`，把 `@图1 是角色1；@图2 是角色2` 安全改写为 `@图2 是角色1；@图1 是角色2`，随后清空草稿，未生成。另以三个未知类型人像和一张本地图真实提交，APP 自动把原提示词 `@图1保持角色一致` 改为心影消息中的 `@图2保持角色一致`；任务进入 RUNNING 后由心影以“图像包含违规内容”正常返回平台失败，证明选择、编号映射、发送和后续任务定位链路均已通过。
- 当前心影项目跨 7 个会话同步出 54 条真正完成且全部可下载的视频；3 个使用 `_video` 容器显示的“今日限流/模型切换提醒”已被排除。真实另存一条 15.073 秒 MP4 为 2,302,423 字节；两条批量下载共 4,581,272 字节。结果 ID 与平台任务 ID均无重复。
- 结果库实机渲染 54 张卡片；播放器打开、下一条切换、关闭后最后查看卡高亮均通过。UI 双选后“标记”显示 2 项标记，再“取消标记”恢复为 0；选择状态也恢复为空。
- 自动验证基线：5 个测试文件、45 项测试通过；`npm run typecheck`、`npm run build`、`npm run verify:cli` 通过。最终安装包为 `release-0.4.0-final/Xinying-Director-0.4.0-x64.exe`，大小 `117,961,338` 字节，SHA-256 为 `89ACD69FB6FC9A8289CA9F02E13815BF6793F4CADA91FD5F8B4B30F40F9BDA10`。

## 真实平台最终验收结论

新生成任务的上传、参数配置、单次提交、对话配对、运行状态、结果识别、自动保存、结果库显示和 CLI 另存均已有实机基线。0.3.1 的统一素材面板与 0.3.2 的共享人像多选、全选、最新/最早排序、权限隔离和双重确认均完成实机验收。根据本次明确的无删除验收边界，真实永久删除分支只完成 DOM、权限、单元测试与禁用态验证，未点击最终删除；功能交付不依赖一次真实删除。

参考图实测使用的纯色编号测试图保存在 `test-results/reference-fixtures/`，不含用户或公司素材。
